// 640px, 832px, 1024px
export const breakpoints = ['640px', '832px', '1024px']

export const devices = {
  mobile: `(min-width: ${breakpoints[0]})`,
  tablet: `(min-width: ${breakpoints[1]})`,
  desktop: `(min-width: ${breakpoints[2]})`,
}
